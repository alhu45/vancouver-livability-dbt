# 1. PROJECT IDENTITY
name: 'vancouver_livability'
version: '1.0.0'
config-version: 2

# 2. CONNECTION
profile: 'vancouver_livability'

# 3. WHERE YOUR FILES LIVE
model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

# 4. CLEANUP
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

# 5. HOW TO BUILD YOUR MODELS
models:
  vancouver_livability:
    staging:
      +materialized: view
      +schema: SILVER
    gold:
      +materialized: table
      +schema: GOLD